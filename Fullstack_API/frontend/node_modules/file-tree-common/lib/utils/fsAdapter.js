'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

exports.default = function (tree) {
  return function (methodName) {
    for (var _len = arguments.length, args = Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }

    switch (methodName) {
      case 'writeFile':
        {
          var filePath = args[0];

          tree.addFile(filePath, { loading: true });
          break;
        }
      case 'mkdir':
        {
          var _filePath = args[0];

          tree.addDir(_filePath, { loading: true });
          break;
        }
      case 'rename':
        {
          var oldPath = args[0];
          var newPath = args[1];

          tree.move(oldPath, newPath);
          break;
        }
      case 'remove':
        {
          var _filePath2 = args[0];

          var node = tree.get(_filePath2);
          if (node) {
            if (node.type === 'directory') {
              tree.removeDir(_filePath2);
            } else {
              tree.removeFile(_filePath2);
            }
          }
          break;
        }
    }
  };
};